{
  "name": "psd2svg",
  "version": "0.1.0",
  "author": "lezgomatt",
  "private": true,
  "scripts": {
    "test": "jest",
    "coverage": "jest --coverage"
  },
  "dependencies": {
    "psd": "^3.2.0"
  },
  "devDependencies": {
    "jest": "^26.6.3"
  },
  "jest": {
    "transformIgnorePatterns": ["/node_modules/(?!(psd)/)"],
    "transform": { ".coffee$": "<rootDir>/jest-coffee-preprocessor.js" }
  }
}
